# ChatBridge Product TODO

## P0

- [x] 微信侧会话可见性补强
  当前 `/list` 只显示会话名和后端，切换前缺少最近活动与内容摘要，用户容易盲切。
- [x] 微信侧会话预览命令
  增加 `/preview [name]`，允许在微信里查看当前或指定会话最近几轮摘要。
- [x] 微信侧 Agent/工程目录信息补强
  `/status` 与 `/agent` 需要显示当前 Agent 的 backend、model、workdir，避免用户不知道自己在操作哪个工程。

## P1

- [x] 增加基础会话生命周期管理
  已支持会话重命名、删除指定会话、按最近活动排序。
- [x] 提供 ChatBridge MCP 管理层
  已支持独立控制平面、进入/退出管理模式、桥命令代理、目标发送方快照、Agent 委派与新会话启动。
- [x] 增加会话分页与批量管理
  已支持按页查看、按名称或摘要筛选，以及批量删除指定会话和清理空会话。
- [x] 增加基础任务执行控制
  已支持任务重试，以及取消排队中的任务。
- [x] 增强任务执行控制
  已支持排队中即时提示、执行中状态反馈、更细的失败恢复，以及运行中任务中断。
- [x] 提供模型级动态切换
  已支持微信侧按会话查看、切换和重置模型，默认仍跟随 Agent 配置。
- [x] 明确 Agent / Backend / Model / Session 关系
  已通过 `/context`、`/status`、MCP guide、管理快照和桌面快速使用区统一解释当前上下文。

## P2

- [x] 增加项目空间管理
  已支持列出可选工程目录、校验目录有效性，并在会话级绑定、切换或重置项目空间。
- [x] 增加命令转义
  已支持双斜杠透传，允许发送以 `/` 开头的普通 prompt。
- [x] 增强会话历史能力
  已支持历史摘要和会话导出；归档与恢复留待后续独立数据模型完善。
